[project]
name = "fastapi-pr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kalyan", email = "kalyanrambhukya54@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "requests>=2.34.2",
]

[tool.vercel]
entrypoint = "src.flaskapi_pr.student:app"

[project.scripts]
fastapi-pr = "fastapi_pr:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
